#include <stdio.h>
#include <stdlib.h>
#include <time.h>
    int main(){
            int idade;
            int tempoSono;
            system("cls"); //limpar telas
            printf("digite a sua idade:\n");
            scanf("%d", &idade);

            tempoSono = (int)(idade/3);

            printf("Voce ja dormiu %d anos\n", tempoSono);

        return 1;
    }


//sgeufn para acc's e se cadastrar